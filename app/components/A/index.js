/**
 * A link to a certain page, an anchor tag
 */

import styled from 'styled-components';

const A = styled.a`
  color: #E50075;
  display: flex;
  justify-content: center;
  margin-top: 21px;
  margin-bottom: 28px;
`;

export default A;
